<h1><%= @task.title %></h1>
<p><%= @task.details %></p>
<p>(Task <%= @task.completed ? 'is' : 'is not'%> completed.)</p>

<button><%= link_to 'Edit this task', edit_task_path %></button>
<p><%= link_to 'Back to all tasks', tasks_path %></p>
