<h1>My tasks</h1>

<ul>
<% @tasks.each do |task| %>
  <li><%= link_to task.title, task_path(task) %> <%= link_to '🗑️', task_path(task), data: {turbo_method: :delete, turbo_confirm: "Are you sure you want to delete '#{task.title}'?"} %></li>
<% end %>
</ul>

<p><%= link_to '+ Create new task +', new_task_path %></p>
